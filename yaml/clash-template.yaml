# Clash-ALL 优化配置 (自动化模板)
# ========================

proxy-providers:

  Mitce机场:
    url: "${{ secrets.MITCE_URL }}"
    type: http
    interval: 86400
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
    proxy: 直连 

  HK-BEUP:
    url: "${{ secrets.HKBEUP_URL }}"
    type: http
    interval: 86400
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
    proxy: 直连 

  PolYun:
    url: "${{ secrets.POLYUN_URL }}"
    type: http
    interval: 86400
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
    proxy: 直连

  无界云:
    url: "${{ secrets.WUJIE_URL }}"
    type: http
    interval: 86400
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
    proxy: 直连
   
  猫耳:
    url: "${{ secrets.MAOER_URL }}"
    type: http
    interval: 86400
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
    proxy: 直连   

  快猫:
    url: "${{ secrets.KUAIMAO_URL }}"
    type: http
    interval: 86400
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
    proxy: 直连    
    
  BestVPN:
    url: "${{ secrets.BESTVPN_URL }}"
    type: http
    interval: 86400
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
    proxy: 直连    

  牛逼:
    url: "${{ secrets.NIUBI_URL }}"
    type: http
    interval: 86400
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
    proxy: 直连      

# ... (此处往下请粘贴您原本 clash-fallback-all.yaml 从第 101 行开始的所有内容)
