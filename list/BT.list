# BT 下载 — 域名列表（provider 风格，仅规则项，不含动作）
# 用法：在 rule-providers 中引用，rules 中使用 RULE-SET,BT / Domain,DIRECT
# 说明：为避免误匹配，已移除过于泛的关键字匹配（如 DOMAIN-KEYWORD,bt）。
DOMAIN-SUFFIX,tracker.openbittorrent.com
DOMAIN-SUFFIX,opentrackr.org
DOMAIN-SUFFIX,leechers-paradise.org
DOMAIN-SUFFIX,coppersurfer.tk
DOMAIN-SUFFIX,tracker.internetwarriors.net
DOMAIN-SUFFIX,publicbt.com
DOMAIN-SUFFIX,bittorrent.com
DOMAIN-SUFFIX,bittorrent.net

# 若需添加更多 tracker，请在此列出每个域名或使用 IP-CIDR（仅公网 IP，避免私网段）
# 示例（若确认为公网 tracker 可启用）：
# IP-CIDR,203.0.113.0/24

# 注意：该文件仅包含规则项，不要在此文件中写入动作（如 ,DIRECT），
# 动作应在主配置的 rules 中通过 RULE-SET 引用时指定。